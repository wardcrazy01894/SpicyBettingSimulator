import type { ReactElement } from 'react';
import type { BetLegView } from '../../shared/api-types.js';

export function BetLegRow(_props: { readonly leg: BetLegView }): ReactElement {
  throw new Error('not implemented: M7c');
}
