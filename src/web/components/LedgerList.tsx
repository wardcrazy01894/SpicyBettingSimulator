import type { ReactElement } from 'react';
import type { LedgerEntry } from '../../shared/api-types.js';

export function LedgerList(_props: { readonly entries: readonly LedgerEntry[] }): ReactElement {
  throw new Error('not implemented: M7d');
}
